#!/usr/bin/env node
var debug = require('debug')('adopt-a-block-REST');
var server = require('../app');


server.listen(3000, function() {
  console.log('%s listening at %s', server.name, server.url);
});